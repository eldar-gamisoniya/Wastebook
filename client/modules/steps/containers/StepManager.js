import React from 'react';

import Step from './Step';

const StepManager = ({ currentStep }) =>
  <div>
    <Step>Wow</Step>
    I will be displaying all the steps
  </div>;

export default StepManager;
